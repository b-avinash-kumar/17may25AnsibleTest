---
- hosts: all
  become: yes
  tasks:
  - name: pinging....
    ping:
  - name: install apache2
    yum:
      name: httpd
      state: latest
      when: ansible_os_family=="Amazon"
  - name: tree 
    apt:
      name: tree
      state: absent
   

